# 绿色可回收物回收量动态预测与回收体系稳定性分析——建模思路总结

> **赛题**：XJTLU Mathematical Modelling Training 2026 · Problem A
>
> **对象**：上海"沪尚回收"城市绿色回收网络（全市 1.5 万+ 基础回收服务点、4300+ 台智能回收箱、800 个惠民服务点）
>
> **核心方法**：Logistic 动力学建模 → 对称拮抗扩展 → 非线性稳定性分析 → 局部灵敏度分析

---

## 目录

1. [问题背景与建模动机](#1-问题背景与建模动机)
2. [Q1：经典 Logistic 模型（零自由度基线）](#2-q1经典-logistic-模型零自由度基线)
3. [Q2：对称拮抗 Logistic 模型（引入 α, β）](#3-q2对称拮抗-logistic-模型引入-α-β)
4. [Q3：回收体系稳定性分析（平衡点与安全边界）](#4-q3回收体系稳定性分析平衡点与安全边界)
5. [Q4：参数灵敏度分析（OAT ±10% 与弹性）](#5-q4参数灵敏度分析oat-10-与弹性)
6. [模型演化总览](#6-模型演化总览)
7. [后续改进方向](#7-后续改进方向)
8. [经典参考文献线索](#8-经典参考文献线索)

---

## 1. 问题背景与建模动机

### 1.1 实际场景

上海"沪尚回收"体系采用"点—站—场"三级回收模式，已覆盖全市社区。但实际运营中存在以下痛点：

| 痛点 | 表现 | 可能的模型映射 |
|------|------|---------------|
| 智能设备满溢、清运不及时 | 居民投递失败，回收量损失 | **抑制系数 β** |
| 老年群体操作智能设备困难 | 适配性不足，参与率低 | **抑制系数 β** |
| 居民回收习惯养成周期长 | 源头分类不精细，增长乏力 | **固有增长率 γ 偏低** |
| 低价值可回收物回收意愿低 | 阶段性回落风险 | **抑制系数 β** |
| 站点覆盖率提升、宣传力度加大 | 正向促进回收量 | **促进系数 α** |

### 1.2 建模哲学

本系列模型遵循**逐步复杂化（Progressive Complexity）**的方法论：

- **Q1**：最简假设 → 诊断问题（$R^2 = 0.532$，证实模型需扩展）
- **Q2**：引入双参数 → 验证结构（$R^2 = 0.9915$，解耦结构成立）
- **Q3**：稳定性理论 → 安全边界（全局结构稳定，但存在性能退化风险）
- **Q4**：灵敏度分析 → 调控优先级（$K$ 主导一切，$\alpha$ 弹性近零）

每一步的模型改进都由**前一步的缺陷诊断**驱动，形成完整的建模逻辑链。

---

## 2. Q1：经典 Logistic 模型（零自由度基线）

### 2.1 模型假设

1. **同质性假设**：回收量 $x(t)$ 是时间 $t$ 的连续可微函数，忽略日内波动与个体异质性。
2. **密度依赖增长**：增长率与当前回收量成正比（"已有基础越大，增长越快"），同时受剩余容量空间约束。
3. **恒定环境**：固有增长率 $\gamma$ 与饱和容量 $K$ 在观测期内保持不变。
4. **无外部调制**：忽略所有促进（宣传、站点扩张）和抑制（清运延迟、设备故障）的外部效应。
5. **封闭系统**：回收量仅受内生增长与容量约束，不考虑跨区流动或政策突变。

### 2.2 ODE 推导

设 $x(t)$ 为 $t$ 时刻的日均可回收物回收量（吨/天）。经典 Logistic 假设：

- **增长项**：$\gamma x$——增长率与存量成正比（丰富的回收基础产生更多回收量）
- **抑制项**：$1 - x/K$——剩余容量比例，当 $x \to K$ 时趋近于零

两者相乘得到经典 Logistic 方程（Verhulst, 1838; Pearl & Reed, 1920）：

$$
\boxed{\frac{dx}{dt} = \gamma \cdot x \cdot \left(1 - \frac{x}{K}\right), \qquad x(0) = x_0} \tag{1}
$$

**经典出处**：此方程最初由比利时数学家 Pierre François Verhulst 于 1838 年提出，用于修正 Malthus 指数增长的无限性，是种群生态学和创新扩散理论中最基础的 S 形增长模型。

### 2.3 解析求解

式 (1) 为可分离变量的一阶 ODE。使用**定积分**从初值条件出发，直接将 $x_0$ 嵌入积分下限：

分离变量后对两端同时作定积分——

$$
\int_{x_0}^{x(t)} \frac{dx}{x(1 - x/K)} = \int_0^t \gamma \, d\tau \tag{2a}
$$

左端被积函数经部分分式展开：

$$
\frac{1}{x(1 - x/K)} = \frac{1}{x} + \frac{1/K}{1 - x/K} \tag{2b}
$$

代入 (2a) 并逐项积分：

$$
\int_{x_0}^{x(t)} \left(\frac{1}{x} + \frac{1/K}{1 - x/K}\right) dx = \gamma t \tag{2c}
$$

$$
\Bigl[\ln x - \ln(1 - x/K)\Bigr]_{x_0}^{x(t)} = \gamma t \tag{2d}
$$

代入上下限，利用对数性质合并：

$$
\ln\left(\frac{x(t)}{1 - x(t)/K}\right) - \ln\left(\frac{x_0}{1 - x_0/K}\right) = \gamma t \tag{2e}
$$

$$
\ln\left(\frac{x(t)}{K - x(t)} \cdot \frac{K - x_0}{x_0}\right) = \gamma t \tag{2f}
$$

两边取指数，令 $A \equiv \dfrac{K}{x_0} - 1 = \dfrac{K - x_0}{x_0}$（注意 $A > 0$，因 $x_0 < K$）：

$$
\frac{x(t)}{K - x(t)} \cdot A = e^{\gamma t}
\;\Rightarrow\;
\frac{K - x(t)}{x(t)} = A e^{-\gamma t}
\;\Rightarrow\;
\frac{K}{x(t)} - 1 = A e^{-\gamma t} \tag{2g}
$$

移项整理得 Logistic 方程的经典解析解：

$$
\boxed{x(t) = \frac{K}{1 + A \cdot e^{-\gamma t}}, \qquad A = \frac{K}{x_0} - 1} \tag{2}
$$

**推导要点**：定积分路径 (2a) 避免了不定积分 $+C$ 再"代入确定常数"的迂回——
初值 $x_0$ 天然地作为积分下限进入推导，物理意义清晰：
$\int_{x_0}^{x(t)}$ 表示从初始状态到当前状态的累积变化，
$\int_0^t \gamma d\tau = \gamma t$ 表示恒定增长驱动力在 $[0,t]$ 上的累积作用。
此外，在定义域内 $0 < x_0 < x(t) < K$，所有对数值均为正数，$\ln|\cdot| = \ln(\cdot)$，无需求助于绝对值讨论。

**数学性质**：

| 性质 | 表达式 |
|------|--------|
| 拐点 | $x = K/2$，此时 $d^2x/dt^2 = 0$ |
| 增长速率最大值 | $\max(dx/dt) = \gamma K/4$ |
| 渐近行为 | $\lim_{t\to\infty} x(t) = K$（饱和） |
| 初始增长 | $dx/dt\|_{t=0} = \gamma x_0(1 - x_0/K)$ |

### 2.4 参数设定

Q1 采用**零自由度**设计：所有参数由题目给定，不作任何拟合。

| 参数 | 符号 | 数值 | 单位 | 来源 |
|------|------|------|------|------|
| 饱和容量 | $K$ | 8011 | 吨/天 | 表 (a) 给定 |
| 固有增长率 | $\gamma$ | 0.022 | 天$^{-1}$ | 表 (a) 给定 |
| 初始回收量 | $x_0$ | 6314 | 吨/天 | 表 (a) 给定 |
| 导出常数 | $A$ | 0.2688 | — | $A = K/x_0 - 1$ |

### 2.5 拟合结果与问题诊断

| 指标 | Q1 值 | 评价 |
|------|-------|------|
| $R^2$ | **0.5319** | 仅解释 53% 方差 |
| MAE | **315.6** 吨/天 | 平均误差超 300 吨 |
| MAPE | 4.42% | 初期误差大 |

**系统性偏差**：全部 9 个残差均为负值（模型高估 $x(t)$），最大偏差出现在 $t = 60$ 天（$-599.4$ 吨/天）。

**诊断结论**：

1. $\gamma = 0.022$ 显著**高估**了有效增长率——实际增长被某种**抑制因素**拖慢。
2. 强制 $x(t) \to K = 8011$，但 $t = 365$ 时实际已达 $8002$——若存在**促进效应**，实际饱和值应可超越 $K$。
3. 模型需要两个新参数：一个**降低有效增长率**，一个**扩展有效容量**。

---

## 3. Q2：对称拮抗 Logistic 模型（引入 α, β）

### 3.1 模型假设的扩展

在 Q1 基础上新增两条关键假设：

1. **促进-抑制解耦假设**：促进效应（$\alpha$）与抑制效应（$\beta$）作用于 Logistic 模型的**正交维度**——$\alpha$ 仅调制饱和容量 $K_{\text{eff}}$，$\beta$ 仅调制有效增长率 $r$。两者互不干扰。

2. **对称拮抗假设**：$\alpha$ 和 $\beta$ 在模型中享有对等的数学地位——均以 $(1 + c)^{\pm 1}$ 的函数形式（$c$ 为对应系数）嵌入模型，拮抗方向相反：
   - $\alpha \geq 0$（促进）：$K_{\text{eff}} = K(1+\alpha)$，**推高饱和平台**
   - $\beta \geq 0$（抑制）：$r = \gamma/(1+\beta)$，**拖慢趋近速度**

3. **非负性约束**：$\alpha \geq 0, \beta \geq 0$——促进和抑制系数的定义保证其物理方向。当 $\alpha = \beta = 0$ 时退化回 Q1。

4. **参数层级**：$\gamma$ 和 $K$ 为**基础固定参数**（外生给定，反映物理上限），$r$ 和 $K_{\text{eff}}$ 为**有效运行参数**（由基础参数和调制系数共同决定），$\alpha$ 和 $\beta$ 为**拟合参数**（由实测数据反推）。

### 3.2 ODE 推导

从 Q1 的 ODE (1) 出发，分别将促进和抑制嵌入对应位置：

$$
\frac{dx}{dt} = \underbrace{\frac{\gamma}{1+\beta}}_{\text{有效增长率 } r} \cdot x \cdot \left(1 - \frac{x}{\underbrace{K(1+\alpha)}_{\text{有效容量 } K_{\text{eff}}}}\right) \tag{3}
$$

**结构对称性**：$\alpha$ 和 $\beta$ 均以 $1 + c$ 形式出现在分母/分子，但 $c$ 的符号效应相反——$\alpha$ 在容量因式中为正贡献（乘数），$\beta$ 在增长率因式中为负贡献（除数）。这种对称拮抗设计保证了模型的**结构性解耦**和**退化一致性**。

化简为标准 Logistic 形式：

$$
\boxed{\frac{dx}{dt} = r \cdot x \cdot \left(1 - \frac{x}{K_{\text{eff}}}\right)} \tag{4}
$$

其中：

$$
r = \frac{\gamma}{1+\beta}, \qquad K_{\text{eff}} = K(1+\alpha) \tag{5}
$$

解析解（与 Q1 同构，仅参数替换）：

$$
\boxed{x(t) = \frac{K_{\text{eff}}}{1 + \left(\dfrac{K_{\text{eff}}}{x_0} - 1\right) e^{-r t}}} \tag{6}
$$

### 3.3 参数识别

式 (6) 含两个独立拟合参数 $r$ 和 $K_{\text{eff}}$。采用 **Levenberg-Marquardt 非线性最小二乘法**（`scipy.optimize.curve_fit`）对表 (b) 的 9 个实测数据点进行拟合。

拟合后反推 $\alpha, \beta$：

$$
\beta = \frac{\gamma}{r} - 1, \qquad \alpha = \frac{K_{\text{eff}}}{K} - 1 \tag{7}
$$

由于 $\gamma = 0.022$，预期 $r \leq \gamma \Rightarrow \beta \geq 0$；$K_{\text{eff}} \geq K \Rightarrow \alpha \geq 0$。拟合结果自动满足非负性约束。

### 3.4 拟合结果

| 类别 | 符号 | 拟合值 | 不确定性（1σ） | 单位 |
|------|------|--------|---------------|------|
| **固定** | $K$ | 8011 | — | 吨/天 |
| **固定** | $\gamma$ | 0.022 | — | 天$^{-1}$ |
| **固定** | $x_0$ | 6314 | — | 吨/天 |
| **拟合** | $r$ | **0.008485** | $\pm 0.000655$ | 天$^{-1}$ |
| **拟合** | $K_{\text{eff}}$ | **8142.22** | $\pm 72.77$ | 吨/天 |
| **导出** | $\alpha$ | **0.016379** | — | — |
| **导出** | $\beta$ | **1.592906** | — | — |

### 3.5 物理解释

| 发现 | 数值证据 | 物理含义 |
|------|----------|----------|
| 促进效应存在但微弱 | $\alpha = 0.0164 > 0$ | 站点覆盖与宣传使有效容量扩展 $1.64\%$（$8142 > 8011$） |
| 抑制效应占据主导 | $\beta = 1.59 \gg 0$ | 运营摩擦使有效增长率降至固有值的 $38.6\%$（$0.0085/0.022$） |
| 增长被严重拖慢 | $r/\gamma = 0.386$ | 清运延迟、适老化不足等负面因素的实际代价是损失了约 61% 的增长潜力 |

### 3.6 拟合效果

| 指标 | Q1（无 α, β） | Q2（含 α, β） | 改善 |
|------|-------------|-------------|------|
| $R^2$ | 0.5319 | **0.9915** | +86.4% |
| MAE | 315.6 | **38.3** | −87.9% |
| MAPE | 4.42% | **0.53%** | −88.0% |

**结论**：双参数调制将模型从"几乎无用"提升至"高度可信"，验证了促进-抑制解耦假设。

### 3.7 经典参照

本模型的对称拮抗结构灵感来源于：

- **Richards 增长模型**（Richards, 1959）：$dx/dt = r x (1 - (x/K)^m)/m$，通过形状参数 $m$ 在 Gompertz、Logistic、monomolecular 之间插值
- **竞争-共生 Lotka-Volterra 扩展**：多物种/多因素交互系统中的对称调制思想
- **Cobb-Douglas 型乘性调制**：$K_{\text{eff}} = K(1+\alpha)$ 和 $r = \gamma/(1+\beta)$ 均采用 $(1+c)^{\pm 1}$ 形式，类比经济学中弹性系数的乘性结构

---

## 4. Q3：回收体系稳定性分析（平衡点与安全边界）

### 4.1 问题转化

背景中"回收量断崖下跌、体系失衡、站点运维失效导致的回收网络瘫痪"对应数学上的**平衡点失稳**问题。

将 Q2 模型 (4) 视为一维自治动力系统：

$$
\dot{x} = f(x) = r x \left(1 - \frac{x}{K_{\text{eff}}}\right), \qquad r > 0,\; K_{\text{eff}} > 0 \tag{8}
$$

### 4.2 平衡点求解

令 $f(x) = 0$：

$$
r x \left(1 - \frac{x}{K_{\text{eff}}}\right) = 0 \quad\Rightarrow\quad
\boxed{x_1^* = 0,\;\; x_2^* = K_{\text{eff}}} \tag{9}
$$

### 4.3 线性稳定性分析（Lyapunov 第一方法）

计算 Jacobi 导数：

$$
f'(x) = \frac{d}{dx}\left[r x - \frac{r}{K_{\text{eff}}} x^2\right] = r\left(1 - \frac{2x}{K_{\text{eff}}}\right) \tag{10}
$$

在平衡点处求值：

| 平衡点 | 特征值 $\lambda = f'(x^*)$ | 符号 | 稳定性 |
|--------|---------------------------|------|--------|
| $x_1^* = 0$ | $\lambda_1 = r$ | $> 0$ 恒成立 | **不稳定** |
| $x_2^* = K_{\text{eff}}$ | $\lambda_2 = -r$ | $< 0$ 恒成立 | **渐近稳定** |

**相图解释**：

- $x \in (0, K_{\text{eff}})$：$\dot{x} > 0$ → 回收量单调递增趋向 $K_{\text{eff}}$
- $x > K_{\text{eff}}$：$\dot{x} < 0$ → 回收量单调递减趋向 $K_{\text{eff}}$
- $x_1^* = 0$ 是排斥子：一旦有初始回收量（$x_0 > 0$），系统绝不会回落至零

**经典参照**：一维 ODE $\dot{x} = f(x)$ 的 Lyapunov 线性稳定性判据——
$\lambda = f'(x^*) < 0 \Rightarrow$ 渐近稳定，$\lambda > 0 \Rightarrow$ 不稳定。
见 Strogatz (2018) *Nonlinear Dynamics and Chaos*, Chapter 2。

### 4.4 结构稳定性

**关键约束**：$\alpha, \beta \geq 0$ 是参数**定义**（促进和抑制系数的物理方向），不是可违反的自由变量。

$$
\beta \geq 0 \;\Rightarrow\; 1+\beta \geq 1 \;\Rightarrow\;
r = \frac{\gamma}{1+\beta} \in (0,\, \gamma] \;\Rightarrow\; r > 0 \;\text{恒成立} \tag{11}
$$

同理，$\alpha \geq 0 \;\Rightarrow\; K_{\text{eff}} \geq K > 0$。

**结论**：

> **系统在整个有效参数空间内具有结构稳定性。**
> 对任意 $\alpha \geq 0, \beta \geq 0$ 和任意正初值 $x(0) > 0$，恒有 $x(t) \to K_{\text{eff}} > 0$。
> **不存在任何分岔点。**

这意味着"沪尚回收"体系不会发生数学意义上的**断崖式崩溃**——只要促进和抑制效应保持其设计定义（恒为正），回收量将始终趋向正平衡值。

### 4.5 实用安全边界

结构稳定性 ≠ 运营安全。系统虽不会崩溃，但性能退化（收敛过慢或容量萎缩）仍需防范。定义**性能约束**：

**增长率约束**（最低可接受恢复速度）：

$$
r = \frac{\gamma}{1+\beta} \geq r_{\min}
\quad\Rightarrow\quad
\boxed{\beta \leq \frac{\gamma}{r_{\min}} - 1} \tag{12}
$$

取 $r_{\min} = 0.005$ 天$^{-1}$（约为基准值的 59%）：$\beta_{\max} = 3.40$。

**容量约束**（最低可接受容量）：

$$
K_{\text{eff}} = K(1+\alpha) \geq K_{\min}
\quad\Rightarrow\quad
\alpha \geq \frac{K_{\min}}{K} - 1 \tag{13}
$$

取 $K_{\min} = K$：$\alpha \geq 0$——由定义自然满足。

**当前安全裕度**：

| 参数 | 当前值 | 安全边界 | 裕度 |
|------|--------|----------|------|
| $\beta$ | 1.59 | $\beta_{\max} = 3.40$ | 1.81（充足） |
| $r$ | 0.0085 | $r_{\min} = 0.005$ | 0.0035（充足，尚有 41% 减速空间） |

**经典参照**：安全边界的定义方法借鉴**工程控制论**中的"性能边界（Performance Boundary）"而非分岔理论中的"稳定性边界"——系统全局稳定，约束来自性能要求而非数学可行性。参见 Åström & Murray (2021) *Feedback Systems*。

---

## 5. Q4：参数灵敏度分析（OAT ±10% 与弹性）

### 5.1 分析目的

Q3 保证了定性安全（不崩溃），Q4 回答定量问题：**参数估计偏差的后果有多严重？调控优先级如何排列？**

### 5.2 方法

**OAT（One-At-a-Time）局部灵敏度分析**：

对每个参数 $p \in \{\gamma, \beta, \alpha, K\}$，令 $p \to p \times (1 \pm 0.10)$，其余参数保持基准值不变，观察输出变化。

**弹性定义**：参数 $p$ 对输出 $y$ 的弹性（无量纲灵敏度）：

$$
\varepsilon_{y,p} = \frac{\partial y / y}{\partial p / p} = \frac{p}{y} \cdot \frac{\partial y}{\partial p}
$$

弹性 > 0 表示同向变化，< 0 表示反向变化。$|\varepsilon|$ 越大越敏感。

### 5.3 解析弹性

由式 (5) 直接求导得解析弹性：

| 输出 | 输入 | 弹性公式 | 弹性值 | 传递特性 |
|------|------|----------|--------|----------|
| $r$ | $\gamma$ | $\varepsilon_{r,\gamma} = \frac{\partial r}{\partial\gamma} \cdot \frac{\gamma}{r} = 1$ | **+1.0000** | 线性传递 |
| $r$ | $\beta$ | $\varepsilon_{r,\beta} = \frac{\partial r}{\partial\beta} \cdot \frac{\beta}{r} = -\frac{\beta}{1+\beta}$ | **−0.6143** | 非线性衰减 |
| $K_{\text{eff}}$ | $\alpha$ | $\varepsilon_{K_{\text{eff}},\alpha} = \frac{\partial K_{\text{eff}}}{\partial\alpha} \cdot \frac{\alpha}{K_{\text{eff}}} = \frac{\alpha}{1+\alpha}$ | **+0.0161** | 近零（$\alpha \ll 1$） |
| $K_{\text{eff}}$ | $K$ | $\varepsilon_{K_{\text{eff}},K} = \frac{\partial K_{\text{eff}}}{\partial K} \cdot \frac{K}{K_{\text{eff}}} = 1$ | **+1.0000** | 线性传递 |

**关键观察**：所有 $|\varepsilon| \leq 1$——**模型在局部线性化意义下是良态的**，不存在微扰导致巨变的乘数效应。最敏感的 $K$ 也不过是按比例传导。

### 5.4 数值弹性（拟合质量）

对 $R^2$ 和 MAE 的弹性无法解析求导（因指标通过 logistic 函数非线性传播），采用**中心差分弧弹性**：

$$
\varepsilon_{y,p}^{\text{down}} = \frac{y(p_{-10\%}) - y_0}{y_0} \Big/ (-0.10), \qquad
\varepsilon_{y,p}^{\text{up}} = \frac{y(p_{+10\%}) - y_0}{y_0} \Big/ (+0.10)
$$

取绝对值的平均值：$\bar{\varepsilon}_{y,p} = (|\varepsilon^{\text{down}}| + |\varepsilon^{\text{up}}|)/2$。

**结果**：

| 参数 | $|\bar{\varepsilon}(R^2)|$ | $|\bar{\varepsilon}(\text{MAE})|$ | 对 $R^2$ 的影响 | 敏感等级 |
|------|---------------------------|----------------------------------|-----------------|----------|
| $K$ | **~96** | **~96** | $R^2$ 从 0.9915 崩塌至 0.26–0.33 | **极高** |
| $\gamma$ | ~0.53 | ~2.8 | $R^2$ 降至 ~0.982 | 低 |
| $\beta$ | ~0.19 | ~1.4 | $R^2$ 降至 ~0.988 | 低 |
| $\alpha$ | ~0.01 | ~0.5 | $R^2$ 几乎不变 | **可忽略** |

### 5.5 $K$ 为何主导一切——数学解释

$K$ 极度敏感的数学根源在于**数据的近饱和特性**：

- $x(0)/K_{\text{eff}} = 77.5\%$：初始时刻已远超 Logistic 拐点（$K_{\text{eff}}/2$）
- $x(365)/K_{\text{eff}} = 98.3\%$：数据几乎全部集中在饱和平台

当 $x$ 接近 $K_{\text{eff}}$ 时，$K_{\text{eff}}$ 的偏差会直接转化为对所有晚期数据点的**系统性同向残差**，导致 $R^2$ 爆炸式下降。而 $\gamma$ 和 $\beta$ 的扰动仅改变曲线的"弯曲程度"，所有扰动曲线最终仍收敛于同一个 $K_{\text{eff}}$——**饱和平台形成了对增长率扰动的天然缓冲**。

### 5.6 $\gamma$ 扰动为何意外地小——非线性衰减机制

直觉上，$\gamma$ 通过 $\exp(-\gamma t/(1+\beta))$ 进入解，应产生非线性放大。但数值弹性 $\bar{\varepsilon}_{R^2,\gamma} \approx 0.53$，远小于 1。

原因：Logistic 在**近饱和区**具有"自压缩"效应。考虑 logit 变换：

$$
\ln\left(\frac{K_{\text{eff}}}{x} - 1\right) = \ln C - rt, \qquad C = \frac{K_{\text{eff}}}{x_0} - 1 \approx 0.29
$$

$r$ 的作用在 logit 空间中是线性的，但映射回 $x$ 空间时经过 sigmoid 函数 $x = K_{\text{eff}}/(1 + e^{\text{logit}})$，其导数在 $x > 0.75 K_{\text{eff}}$ 处急剧衰减：

$$
\frac{dx}{d(\text{logit})} = x\left(1 - \frac{x}{K_{\text{eff}}}\right)
$$

在 $x \approx 0.775 K_{\text{eff}}$ 处，此导数约为 $0.17 K_{\text{eff}}$；在 $x \to K_{\text{eff}}$ 时趋近于 0。**非线性在这里不是放大，而是衰减**——这是 Logistic 在饱和区的基本数学性质。

### 5.7 经典参照

- **Saltelli et al. (2008)** *Global Sensitivity Analysis: The Primer*：局部 OAT 分析的适用性与局限（适用于参数不确定度较小时的初步筛选）
- **Cacuci (2003)** *Sensitivity and Uncertainty Analysis*：弹性定义的数学基础与有限差分近似
- **Hamby (1994)**：灵敏度分析方法综述，OAT 的标准化流程

---

## 6. 模型演化总览

### 6.1 迭代逻辑

```
Q1 (零参数)
  │  $R^2 = 0.532$——模型高估增长、容量假设过强
  │  诊断：需要降低有效增长率 + 扩展有效容量
  ▼
Q2 (双参数调制: α, β)
  │  $R^2 = 0.9915$——拟合质的飞跃
  │  发现：抑制主导（β=1.59≫0），促进微弱（α=0.016≈0）
  │  验证：促进-抑制解耦结构成立
  ▼
Q3 (稳定性分析)
  │  全局结构稳定——不会崩溃
  │  安全边界充裕：β_max = 3.40 ≫ β = 1.59
  │  遗留问题：结构安全 ≠ 参数鲁棒，需要定量灵敏度
  ▼
Q4 (灵敏度分析)
      K 绝对主导（弹性~96），α 近零（弹性~0.01）
      策略转型：精确测 K → 定向降 β → 重新定位 α
```

### 6.2 核心发现矩阵

| 维度 | Q1 | Q2 | Q3 | Q4 |
|------|----|----|----|----|
| **模型自由度** | 0 | 2 (α, β) | 2 | 2 |
| **$R^2$** | 0.532 | 0.992 | — | — |
| **关键参数** | — | β = 1.59 | β 裕度 1.81 | K 弹性 ~96 |
| **主要结论** | 模型不充分 | 解耦结构正确 | 全局稳定 | K 是生命线 |

### 6.3 最终调控策略

```
优先级 1 ─── 精确测量 K（±10% 偏差 → 模型崩溃）
优先级 2 ─── 定向削减 β（×38 倍于 α，聚焦清运/适老化/低价值补贴）
优先级 3 ─── 重新定位 α（从扩张站点→提升服务质量，关注对 γ/β 的间接效应）
优先级 4 ─── 监测 γ（系统鲁棒，季度抽样即可）
```

---

## 7. 后续改进方向

### 7.1 模型结构层面

| 方向 | 描述 | 收益 |
|------|------|------|
| **时变参数** | 将 $\alpha(t), \beta(t)$ 建模为时间的慢变函数（如 $\beta$ 随运维投入递减） | 刻画政策干预的动态效果 |
| **随机微分方程** | 在 ODE 中引入 Wiener 过程项 $dW_t$，将参数不确定性内化为系统的随机波动 | 生成预测置信区间 |
| **多品类分解** | 将 $x(t)$ 按品类（高/低价值、纸/塑/金属等）分解为 $x_i(t)$ 子系统，每类有独立的 $\gamma_i, K_i$ | 解释低价值品类回收意愿的结构性差异 |
| **延迟微分方程** | 引入政策干预的时间延迟 $\tau$：$\dot{x}(t) = f(x(t), x(t-\tau))$ | 捕获宣传推广→行为改变的滞后 |

### 7.2 分析方法层面

| 方向 | 描述 | 收益 |
|------|------|------|
| **全局灵敏度分析** | Sobol' 指数或 FAST 方法，替代 OAT 局部线性化 | 捕获参数交互效应和高阶敏感性 |
| **贝叶斯推断** | 对 $K$ 引入先验分布（基于人口普查、消费数据），通过 MCMC 获取后验 | 量化 $K$ 的认知不确定性 |
| **模型选择准则** | 使用 AIC/BIC 对比 Q1–Q2 及其他备选模型结构 | 为模型复杂度提供统计检验依据 |

### 7.3 数据层面

| 方向 | 描述 | 收益 |
|------|------|------|
| **高频数据** | 获取日度回收量时间序列（当前仅有 9 个点） | 辨识周内/季节性模式，提高拟合精度 |
| **空间数据** | 获取各行政区的分类回收量面板数据 | 识别区域异质性，分级制定策略 |
| **外部协变量** | 接入气象、节假日、政策事件等外生变量 | 解释残差中的结构性波动 |

---

## 8. 经典参考文献线索

| 文献 | 与本模型的关系 |
|------|---------------|
| Verhulst, P.F. (1838). Notice sur la loi que la population suit dans son accroissement. *Corr. Math. et Phys.*, 10, 113–121. | 原始 Logistic 方程的提出 |
| Pearl, R. & Reed, L.J. (1920). On the rate of growth of the population of the United States since 1790 and its mathematical representation. *PNAS*, 6(6), 275–288. | Logistic 模型的独立再发现与统计验证 |
| Richards, F.J. (1959). A flexible growth function for empirical use. *J. Exp. Bot.*, 10(2), 290–301. | 广义 Logistic（引入形状参数）——Q2 对称拮抗结构的远亲 |
| Strogatz, S.H. (2018). *Nonlinear Dynamics and Chaos* (2nd ed.). Westview Press. | 一维动力系统稳定性分析的标准框架（Q3 理论基础） |
| Saltelli, A. et al. (2008). *Global Sensitivity Analysis: The Primer*. Wiley. | 灵敏度分析的方法论标准（Q4 方法论参考） |
| Åström, K.J. & Murray, R.M. (2021). *Feedback Systems: An Introduction for Scientists and Engineers* (2nd ed.). Princeton. | 性能边界与安全裕度的工程视角（Q3+Q4 联合分析） |
| Banks, H.T. & Tran, H.T. (2009). *Mathematical and Experimental Modeling of Physical and Biological Processes*. CRC Press. | 逆问题参数估计方法（Q2 拟合的统计框架） |

---

> **文档信息**
>
> 赛题：XJTLU Mathematical Modelling Training 2026 · Problem A
>
> 问题：绿色可回收物回收量动态预测与回收体系稳定性分析（Q1–Q4）
>
> 模型家族：Classical Logistic → Symmetric Antagonistic Logistic → Stability + Sensitivity
>
> 输出文件：`建模思路总结.md`
